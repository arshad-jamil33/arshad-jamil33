name: Android Build

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Started
        run: echo "ROM build started 🚀"

      - name: Fake Build Step
        run: sleep 30

      - name: Build Finished
        run: echo "Build completed ✅"
